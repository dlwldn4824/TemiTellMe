# 🎉 최종 설치 가이드 - React 화면 띄우기

## ✅ 완료된 작업

1. ✅ **MainActivity를 BridgeActivity로 변경** - Capacitor 방식
2. ✅ **접근 제어자 수정** - `onStart()`, `onDestroy()`를 `public`으로 변경
3. ✅ **React 빌드 완료** - `dist/` 생성
4. ✅ **Capacitor로 안드로이드 복사 완료** - `assets/public/`에 복사
5. ✅ **APK 빌드 성공** - `app-debug.apk` 생성 완료

---

## 🚀 로봇에 설치 및 실행

### 1단계: 로봇 연결

```bash
adb connect 10.200.57.107:5555
adb devices
```

출력 예시:
```
List of devices attached
10.200.57.107:5555	device
```

✅ **"device"가 보이면 연결 성공!**

---

### 2단계: APK 설치

```bash
cd "/Users/LEEJIWOO/Desktop/대학교폴더/2학년 2학기/HCI-UX/co-show/android"

adb install -r app/build/outputs/apk/debug/app-debug.apk
```

출력 예시:
```
Performing Streamed Install
Success
```

✅ **"Success"가 보이면 설치 완료!**

---

### 3단계: 앱 실행

```bash
adb shell am start -n com.example.coshowsample/.MainActivity
```

또는 **로봇 화면에서 직접 앱 아이콘을 터치**해도 됩니다.

---

## 🎯 예상 결과

앱을 실행하면:

1. **BridgeActivity가 자동으로 WebView 생성** ✅
2. **`assets/public/index.html` 로드** ✅
3. **React 앱 화면 표시** ✅
4. **Temi SDK 기능 사용 가능** ✅
5. **WebSocket 연결** ✅

**개발 서버 없이도 로컬에 빌드된 React 앱이 실행됩니다!** 🎉

---

## 🔍 문제 해결

### 문제 1: "adb: no devices/emulators found"

**해결**:
```bash
# 로봇 IP 확인 후 재연결
adb connect 10.200.57.107:5555
adb devices
```

---

### 문제 2: "INSTALL_FAILED_INVALID_APK"

**해결**:
```bash
# 기존 앱 제거 후 재설치
adb uninstall com.example.coshowsample
adb install app/build/outputs/apk/debug/app-debug.apk
```

---

### 문제 3: 화면이 안 뜨거나 빈 화면

**확인**:
1. 로그 확인:
   ```bash
   adb logcat | grep -i "capacitor\|webview\|coshowsample"
   ```

2. `assets/public/index.html` 파일 확인:
   ```bash
   ls -la android/app/src/main/assets/public/
   ```

3. React 빌드 재실행:
   ```bash
   cd co-show
   pnpm run build
   npx cap copy android
   ```

---

### 문제 4: API 호출 실패

**확인**:
- 백엔드 서버가 실행 중인지 확인
- `src/services/apiBase.js`의 IP 주소 확인 (로봇에서 접근 가능한 주소)

---

## 📋 전체 명령어 체크리스트

```bash
# 1. 로봇 연결
adb connect 10.200.57.107:5555
adb devices

# 2. APK 설치
cd "/Users/LEEJIWOO/Desktop/대학교폴더/2학년 2학기/HCI-UX/co-show/android"
adb install -r app/build/outputs/apk/debug/app-debug.apk

# 3. 앱 실행
adb shell am start -n com.example.coshowsample/.MainActivity

# (선택) 로그 확인
adb logcat | grep -i "capacitor\|webview\|coshowsample"
```

---

## 🎊 성공 확인

앱 실행 후:
- ✅ React 메인 화면이 로봇 화면에 표시됨
- ✅ 네비게이션 작동
- ✅ Temi SDK 기능 사용 가능 (이동, 말하기 등)
- ✅ WebSocket 연결 정상

**이제 로봇에서 완전한 React 앱을 실행할 수 있습니다!** 🚀



